version: '3.8'

services:
  flask_app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - .:/app  # Montre le code source pour un rechargement en direct
    environment:
      - FLASK_ENV=development
